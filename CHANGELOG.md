# KOSPI LAB — 변경 이력

> 최신이 위. 배포 URL: https://kospi-lab.vercel.app (git push → Vercel 자동 배포)

## 2026-09-22 (42차) — 더보기 도구 중복 정리

- `EXTRAS`(메뉴 시트 '더보기 도구')에서 **KRX 시장(`/krx`)·성장주 발굴(`/growth`) 제거** — 중복. KRX는 시장›국내
  "전체보기"·국내 탭 alias로, 성장주 스크리닝은 분석›스크리너로 진입. 두 라우트·alias·API는 그대로 유지(페이지 삭제 아님).
- `lib/menu.ts` 한 곳만 수정. tsc 0 · build 0.

## 2026-09-18 (41차) — 관심종목 자동분석·시장탭 모바일 행 정렬·선물 막대그래프·하단바 관심종목 탭

주식3 세션. 사용자 요청 4건.
- **관심종목에서 '분석' 자동 실행**: 관심목록 행/⋮의 분석 링크에 `?run=1` 부여 →
  `/stock-analysis`·`/coin-analysis`가 진입 즉시 자동 분석(모델 준비 후 1회). 스크리너 등 일반 `?ticker=` 딥링크는
  기존대로 버튼 수동 실행 유지(AI·수집 비용 원칙 §0 보존).
- **🐛 시장탭 국내·해외 글씨 잘림**: 목록 행이 데스크탑용 6열 그리드(`40 48 1fr 120 90 140`)를 모바일에서도 써서
  고정 열 합(≈428px)이 화면(≈358px)을 넘겨 **종목명(1fr) 열이 0으로 찌그러져 이름이 안 보이고** 오른쪽 빈 열이 생김.
  → 모바일 `flex`(이름 `flex-1`, 시세는 `sm:contents` 래퍼) / sm+ 기존 grid로 전환. 국내·해외 동일 수정.
- **선물 시각화**: 검색 입력 제거, 상단에 **상승·하락·거래대금 TOP 12 막대그래프**(`HBarChart`, 세그먼트 전환).
  코인 관행(초록=상승)에 맞춰 막대·값 색 지정 — `HBarChart`에 `posText`/`negText` 옵션 추가(주식 페이지 기본은 빨강 유지).
- **하단바 '관리' → '관심종목' 탭**: `/my-stocks`를 홈에서 분리해 4번째 탭으로 승격(국내·해외·코인 저장 목록 한 곳).
  매매 규율 4종(플래너·매매일지·통합 리스크·목표 수익률)은 **'자산' 탭으로 병합**(상단 가로 서브탭 스크롤). `lib/menu.ts`만 수정.
- 게이트: tsc 0 · 테스트 142 · build 0. (로컬 dev는 기존 globals.css `@import` 배치로 Turbopack만 못 띄움 — build/프로덕션 정상)

## 2026-09-18 (40차) — 남은 화면 전부 새 구조로: 관심종목 목록·해외/코인 상세·스와이프 동작 + 관심목록 동기화 버그

39차 구조를 나머지 화면에 적용. 사용자 결정: 39차의 IA 선택 6가지는 그대로 확정.
- **관심종목 전체 목록** `/my-stocks` — 옛 큰 카드 그리드 → 시장 세그먼트(개수)·검색으로 추가·정렬/시장 칩·고밀도 행.
  **행을 왼쪽으로 밀면 숨은 버튼**(국내·코인=분석, 해외=가상투자 / 삭제), ⋮ 는 전체 동작(상세·분석·가상투자·매매 계획·삭제).
  삭제는 **4초 되돌리기 토스트**. 보유 수량·손익률·알림 여부는 행 보조 문구로, 보유 평가손익 합계 카드. 현재 탭 시장만 호출.
  옛 카드·검색바·필터 부품 6개(StockCard·OverseasStockCard·CryptoCard·SearchBar·OverseasSearchBar·StockFilter) 삭제.
- **해외 상세 전용 화면** `/overseas/[symbol]`(신규) — 모달 대신 드릴다운: 헤더(☆·⋮: 관심·가상투자·매매 계획·Yahoo·공유)
  → 가격 차트(기간·지표 시트) → 52주 위치(접기). 원화 환산. 해외 목록 행·검색·홈 관심종목이 이 화면으로, 옛 `?symbol=` 링크는 리다이렉트.
  차트용 공개 라우트 `/api/overseas/chart`(Yahoo v8 일봉, 5분 캐시). Yahoo 거래소 코드(NMS 등)는 통용명(NASDAQ)으로.
- **코인 상세 재구성** `/crypto/[symbol]` — 같은 계층 + 한글명, 24시간 고·저는 한국 색, **USDT 무기한 선물**(선물가·프리미엄·펀딩비+해설)을 접기 카드로.
- **상세 화면 옆으로 넘기기**(`components/detail/SwipeNav`) — 국내·해외·코인 상세 헤더를 밀면 관심목록의 이전/다음 종목(replace라 뒤로가기는 목록으로),
  아래 "‹ 이전 · 관심 n/N · 다음 ›" 바. 목록에 없는 종목(검색 진입)엔 붙지 않음.
- 공용 부품: `components/detail/PriceChart`(해외·코인 공용 차트 카드, MA5·20·60·거래량 시트), `components/SwipeRow`(밀어서 여는 행).
- **🐛 관심목록 동기화 버그 2건** → 공용 훅 `hooks/useSyncedList`: ① 훅을 쓰는 곳마다 상태가 따로라 **검색 시트에서 ☆로 추가해도
  열린 목록엔 새로고침 전까지 안 보였음** ② add/remove 가 렌더 당시 목록을 참조해 **되돌리기가 "이미 있음"으로 무시**됨.
  저장 시 같은 탭(커스텀 이벤트)·다른 탭(storage)으로 알리고, 조작은 항상 최신 목록 기준. 국내·해외·코인 워치리스트 훅 3종 이관.
- 실측(Pixel 7, 합성 터치 이벤트): 행 밀기→버튼 노출, 삭제→되돌리기 2→3행, 목록 연 채 검색 ☆ 추가 3→4행(새로고침 없음),
  상세 스와이프 005930→035720, 해외 행 클릭→/overseas/AAPL, 옛 링크 리다이렉트, 페이지 에러 0. tsc 0·테스트 142·build 0.

## 2026-09-18 (39차) — IA 5섹션 재편 + 트레이딩앱식 내비·검색·시트·종목 상세 계층

사용자 지정 IA로 전면 재편하고, 트레이딩 앱의 **구조 패턴**(관심목록 칩 전환·행 동작 메뉴·종목 화면 계층·
검색 필터 칩·캘린더 필터 커튼)을 참고해 모바일 탐색을 다시 짰다. 색·아이콘·문구·자산은 가져오지 않음(구조만).
- **IA 5섹션 = 하단 5탭** `lib/menu.ts`(단일 소스): 홈(대시보드·관심종목·경제 캘린더·뉴스) · 시장(국내·해외·코인·선물)
  · 분석(종목 분석·스크리너·공시·리포트) · 관리(플래너·매매일지·통합 리스크·목표 수익률) · 자산(포트폴리오·계좌·성과).
  `alias`(종목 분석=국내+코인선물, 스크리너=스크리너+성장주, 국내=국내+KRX)·`tabExtra`(상세 드릴다운의 탭 강조)·
  `EXTRAS`(가상투자·투자설계·세제·시뮬·증권사 = 메뉴 시트 '더보기 도구'). 접두어 오탐(`/stock`↔`/stock-analysis`) 방지.
- **내비 계층**: 섹션 안 이동 = 앱바 아래 **밑줄 탭**(sticky), 섹션 밖 상세 = 앱바 **뒤로가기**. 앱바는 홈=로고 /
  섹션=큰 제목 / 상세=뒤로+제목, 우측 **검색·전체 메뉴** 아이콘. 중앙 FAB 제거(플래너는 관리 탭·홈 리스크 카드로).
- **통합 검색 시트**(전체 화면): 시장 필터 칩(전체·국내·해외·코인)·시장별 섹션·최근 검색·많이 찾는 종목·
  행 ☆로 **바로 관심종목 추가/해제**. 해외는 `/overseas?symbol=` 딥링크로 상세 모달 직행, 코인은 `lib/coins` 카탈로그.
- **바텀시트·액션시트(⋮)·접기 카드** 공용 부품(`components/ui/`): 드래그로 닫기·ESC·스크롤 잠금·안전영역·포털.
- **홈 4섹션**: 시장 요약(인사·코스피 히어로·주요 지표 가로 스트립) · 관심종목(국내|해외|코인 세그먼트, 고밀도 행,
  ⋮ 액션, 현재 시장만 호출) · 오늘의 리스크(서킷브레이커·오늘 실현·연속 손절·미청산·7일 내 고영향 이벤트) ·
  주요 이벤트(중요도 막대·D-day). 상승/하락 TOP → 시장›국내, 코인 거시·ETF → 시장›코인(신규 `/coins`).
- **종목 상세 계층**: 헤더(☆·⋮ 액션: 관심·종목 분석·매매 계획·지표·네이버·공유) → 차트(기간 세그먼트, **지표·비교는
  시트로 숨기고 켜진 것만 범례**) → 정보(수급·재무 펼침, 상장·기업·배당·AI·보유/알림 접힘).
- **🐛 종목 상세 버그 2건**: ① DART·KRX 섹션이 `ticker.endsWith('.KS')` 조건 때문에 **항상 숨겨져** 있었음
  (라우트는 접미사 없는 6자리) → 6자리 정규식으로 수정. ② 이 화면만 상승=초록/하락=빨강 → 한국 관행(빨강/파랑)으로 통일,
  수급 음수 부호 누락도 보완.
- **경제 캘린더**: 분류 칩 + 국가·중요도·지난 일정은 **필터 바텀시트**, 날짜별 묶음 카드, 오늘 기준을 **KST**로
  (기존 UTC 자르기라 오전 9시 전엔 '어제'가 오늘로 표시되던 문제).
- 신규 **자산›성과** `/performance`(주간 리뷰·코인/주식 성적 카드 — `ScoreCard` 를 매매일지에서 공용 추출),
  **분석›종목 분석** 상단 국내주식|코인선물 전환.
- **다크 모드**: 배경을 한 단 깊게(#0f1115)·표면 대비↑·선 옅게, 테마 색 메타 동기화.
- 실측: Playwright(Pixel 7)로 홈·검색(입력)·메뉴 시트·시장 탭·코인·종목 상세·⋮ 시트·지표 시트·캘린더 필터·성과·
  다크 홈/상세를 **실제 탭 조작**으로 촬영. tsc 0·테스트 142·build 0.

## 2026-09-17 (38차) — 모바일 홈 핀테크 카드 디자인 (레퍼런스 스타일·레이아웃 이식)

사용자 제공 핀테크 앱 레퍼런스의 **디자인 스타일과 레이아웃 패턴**을 모바일 홈에 이식. 아이콘·일러스트는 전부 직접 그림,
데이터는 전부 실데이터. 색 방향은 한국 관행(상승=빨강·하락=파랑) 유지. 데스크탑(md+) 무변경.
- **인사 헤더** `fin/Greeting` — "오늘의 시장 · 9월 17일 (목)" + 장 상태 칩(KST 평일 09:00~15:30, 공휴일 미반영).
- **그라디언트 히어로** `fin/HeroIndex` — 코스피 큰 수치·등락칩·"1개월" 셀렉터 + **1개월 스파크라인**
  (`fin/Sparkline`: 의존성 0 SVG, Catmull-Rom 곡선·영역 채움·고점 말풍선·현재점 링) + 하단에 **겹쳐 앉는 흰 3분할 스탯 바**(코스닥·코스피200·나스닥).
  데이터: 신규 공개 라우트 `/api/index-spark`(Yahoo ^KS11 1개월 일봉, 5분 캐시 — 10초 폴링되는 /api/market과 분리).
- **프로모 배너 자리** `fin/MarketBanner` — 판촉 대신 규율 리마인더("진입 전에 손절·사이징부터") → 플래너. 막대+방패 체크 원본 일러스트.
- **상승률/하락률 TOP** `fin/RankList` — 좌측 컬러 바 리스트 행(KRX 공식 랭킹, 기준일 표기), 행 탭 → 종목 상세.
- **시장환경 2열 미니카드** `MarketEnvGrid` 모바일 변형 — 아이콘 배지(%·기름방울·$·₩·깃발·게이지·달력) + 라벨 + 큰 값 + 톤 칩.
  ⚠ 칩 색은 가격 방향이 아니라 '위험자산 우호/불리'(기존 의미 유지). 콜드 로드 때 헤더+카드 모양 시머로(빈 박스 방지).
- **탭바 중앙 FAB** — 홈·매매·**[+ 새 매매 계획]**·시세·더보기(`TAB_GROUP_KEYS` 단일 소스). 내 자산은 더보기로 접힘.
- 관심종목 행·역할 안내도 핀테크 카드로. `globals.css`에 `.fin-*`·`.tint-*`·`.app-fab` 디자인 언어 추가.

## 2026-09-17 (37차) — 모바일 네이티브 앱 셸 (APK 유료앱 수준 1단계)

APK는 TWA(배포 URL 로드형)라 **웹 모바일 UI = 앱 UI**. Playwright(Pixel 7) 실측에서 "웹페이지처럼" 보이던 원인
— 매 화면 상단 '☰ 메뉴' 카드(바텀시트)·페이지별 앱바 없음·벽 텍스트 푸터 반복·텍스트 로딩 — 을 셸 교체로 해소.
토스 토큰(팔레트·카드·폰트)은 유지, **셸만 바꿔 28페이지에 일괄 반영**. 데스크탑(md+)은 무변경.
- **하단 고정 탭바** `components/BottomNav.tsx` — 홈·매매·시세·내자산·더보기 5탭(lib/menu 4그룹 단일 소스,
  그룹 탭=첫 항목·활성=matchFn). 블러 표면·활성 알약 인디케이터·눌림 축소·`env(safe-area-inset-bottom)` 제스처바 여백.
- **페이지별 앱바** `Header.tsx` — 홈=로고+검색 / 내부=뒤로가기+제목(메뉴 라벨 자동) / **탭 루트(홈·더보기·그룹 첫 화면)는
  뒤로가기 숨김**(네이티브 관례). 데스크탑 헤더는 `hidden md:flex`로 그대로.
- **그룹 서브칩 가로 스크롤** `NavTabs.tsx` — 모바일 '메뉴' 카드·바텀시트·드래그 코드 제거 → 활성 그룹 하위 항목만
  칩으로(정보·도구는 더보기 탭이 대신). 방문 빈도 기록은 유지.
- **더보기 허브** `app/more/page.tsx` — HomeMenu(검색·자주 쓰는·전체 4그룹) + 설정 리스트 행(테마·동기화·이용가이드).
- **홈** — 상단 퀵액션 4종(목표·플래너·매매일지·리스크, 팝업 제거 보완) · '이 도구의 역할'을 한 줄+접기(정직성 문구 유지)
  · 히어로 "불러오는 중…" → 스켈레톤 시머.
- **푸터** — 모바일은 출처 한 줄+`<details>` 접기, 데스크탑 전문 유지. 탭바에 가리지 않게 하단 여백.
- `globals.css` — `.app-tabbar/.app-tab/.appbar/.chip-scroll/.skeleton/.list-row` + 탭 하이라이트 제거·overscroll 차단.
  `viewportFit: 'cover'`(안전영역 활성). 다음 단계 후보: 분석 화면 하단 고정 CTA·리스트 행 전환·햅틱형 전이.

## 2026-09-17 (36차) — 데이터 표시 정합 3건(호가틱·부동소수·PEG 클램프)

35차 검증에서 남긴 미세 개선 3건 정리(전부 표시·정합, 엔진 순수성·테스트 불변).
- **주식 진입·손절·목표 호가 틱 반올림** — 242,894.125원 같은 무효 호가 → KRX 틱단위(2천~50만·이상 구간별)로
  반올림해 실제 주문 가능한 가격으로. stopPct도 2자리. 표시 계층(라우트)에서만 처리(엔진·백테스트·테스트 불변).
- **코인 진입·손절·목표 부동소수 잔재 제거** — 76051.45000000001 → 가격 규모별 유효자리 반올림, rr·stopPct 2자리(라우트).
- **PEG ±300 클램프 아티팩트 격리** — 컨센 EPS 성장률이 `growthPct` 상한(±300%)에 걸리면 PEG 분모가 잘려
  초저 PEG(0.02)가 되던 것 → 그 경우 '저평가성장' 배지·밸류 가점에서 제외 + "성장률 상한 걸림, PEG 신뢰 불가" 경고.
  성장 자체는 growth·outlook 점수에 이미 반영되므로 이중가점 방지. (35차의 <0.15 "과대추정" 경고는 유지)
- **`/api/market` changeRate 반올림 통일** — 지수는 소스가 반올림·환율/나스닥은 full-float이던 불일치를 `r2`로 2자리 통일.

## 2026-09-17 (35차) — 데이터 오류 전수 검증 + 🐛 시가총액·거래대금 "-" 수정

앱 전 데이터 소스를 원본과 교차검증(관세청·FRED·KRX·Yahoo·Bitget·Upbit). **소스 대비 오류 0** —
반도체 +203%·WTI $107·Brent $130 등 극단값도 전부 원본과 정확 일치(타임라인 실제값). 발견한 유일한 앱 버그 수정.
- **🐛 시가총액·거래대금 "-"** — 네이버 모바일 종목 API가 키를 축약형(`시총`·`대금`)으로 바꿔
  `get('시가총액')`/`get('거래대금')`이 항상 "-" 반환(국내주식 분석 상세에서 두 값이 계속 비어 있었음).
  `pick('시총','시가총액')`·`pick('대금','거래대금')` 폴백으로 수정(구키 복귀도 대비). 거래량·52주·PER·PBR은 키가 맞아 무영향.
- **검증으로 버그 아님 확정**: 백테스트 0건(삼성·하이닉스)은 급등주라 `posInRange<0.97`+`score≥40`이 상충하는 정상 동작
  (다른 종목은 신호 산출·대규모 측정은 별도 스크립트) · 김프 0.65%·롱숏비 1.83 수식 일관 · 외국인보유율 46.52% 정상(NaN 아님).
- 미세 개선 여지(미수정): stop/target 원 단위 미반올림(242894.125원)·PEG ±300 클램프 배지·changeRate 반올림 불일치.

## 2026-09-17 (34차) — 공용 캐시(TTL+LRU) 통일 + 분석 데이터 결측 공시

완성도 점검 잔여 2건을 정리(축3 캐시 누수 소지, 축4 부분 실패 미공시).
- **공용 `lib/cache.ts`(TtlCache)** — TTL 만료 자동삭제 + 용량 상한 LRU 축출 + 부정 결과(null/[]) 캐시.
  종목·티커 키로 **무제한 성장**하던 캐시들을 이관: `dartClient`(4)·`naverFinance`·`growthScreener`·
  `kisFinance`(2)·`yahooFinance` + 분석 라우트 로컬 백테스트/AI 캐시(주식·코인 각 2). 각자 손으로 짜던
  만료·축출 로직을 한 곳으로. (단일슬롯·고정키 소수 캐시는 이미 유계라 유지) — 회귀 `tests/cache.test.ts` 8건.
- **분석 결측 공시** — 소스 수집 함수들이 외부 API 실패를 삼키고 빈 값을 돌려줘, 판정이 **결측 위에서**
  계산돼도 화면엔 안 보이던 문제. 응답에 `sources`(소스별 성공/실패·핵심여부) 추가 + `SourceStatus` 배너로
  국내주식·코인선물 분석 결과 상단에 "무엇이 빠졌고 판정이 그 위에서 나왔다"를 정직하게 표시.
  주식: 수급·재무·코스피·경제지표·공시·뉴스 / 코인: 롱숏·테이커·OI·오더북·공포탐욕·김프·뉴스.
- **AI 브리핑·복기 코치 복구** — 무효(401)였던 `ANTHROPIC_API_KEY`를 유효 키로 교체·재배포(models 200 검증).
- **텔레그램 알림 가동** — 신호 크론용 `TELEGRAM_BOT_TOKEN`·`TELEGRAM_CHAT_ID` GitHub Secrets 등록(봇 getMe·발송 검증).

## 2026-09-16 (33차) — 메뉴 간소화·통일: 단일 소스 4그룹 IA

"메뉴가 여기저기 너무 많아 헷갈린다" → 내비·모바일 팝업·홈 카드가 제각각 그룹을 정의하던 것을
**단일 소스 `lib/menu.ts`**로 통합하고, 의도(할 일) 기준 **4그룹**으로 재편.
- **매매**(핵심): 목표 수익률·플래너·매매일지·통합 리스크·국내주식 분석·코인선물 분석
- **시세**: 국내주식·해외주식·코인·선물·KRX 시장
- **내 자산**: 통합 자산·내 주식·비트겟·가상투자
- **정보·도구**: 뉴스·공시·리포트·캘린더·성장주·스크리너·투자설계·세제·시뮬·증권사(자주 안 쓰는 참고/발굴/설계를 한 곳에)
- 이 앱의 정체성인 '매매 규율'을 맨 앞에. 기존 비대했던 '분석 12개'를 성격별로 해체.
- NavTabs·HomeMenu·page(홈 카드) 모두 lib/menu 소비 — 그룹/아이콘/매칭 한 곳에서 관리. 홈 카드도 이모지→라인 SVG 통일.
- 그룹 matchFn은 항목 href로 자동 생성(손수 매칭 제거). 실브라우저 1512px 확인.

## 2026-09-16 (32차) — PC 시인성 개선(초소형 텍스트 확대 + 보조색 대비)

PC 큰 화면에서 텍스트가 작고 흐릿하다는 피드백 → 전역 개선(컴포넌트 무수정).
- **데스크탑 폰트 확대**(≥1024px): 앱이 모바일 우선이라 `text-[10px]`(352곳)·`[11px]`(129)·`[9px]` 등이 많음.
  globals.css unlayered 규칙으로 8~13px 및 text-xs를 데스크탑에서만 상향(모바일 불변, line-height 미변경).
- **보조 텍스트 대비 상향**(라이트만): `--muted`/`--text-muted` #6b7684→#4d5665, `--faint`/`--text-dim` #8b95a1→#6b7684.
  다크 모드 토큰은 별개라 영향 없음. 실브라우저 1486px 확인.

## 2026-09-16 (31차) — 거래소에서 손절가 자동복구 + 비트겟 청산내역 표

비트겟 USDT 선물의 진입·청산·손익·시간은 이미 거래소에서 가져오고 있었고(거래소 대조),
여기에 **손절가 복구**와 **청산내역 표**를 추가.
- **손절가 자동복구**: `/api/bitget/history`가 SL(스탑로스) 주문 이력(orders-plan-history, planType=profit_loss)을
  같이 읽어, 순수 함수 `attachStops`로 청산 포지션에 매칭(심볼+시간창+손절방향, side 필드 미신뢰). best-effort — 없거나
  권한 없으면 손절 복구만 skip.
- **거래소 R 환산**: `exchangeRiskUsdt = |진입−손절|×수량`. reconcile이 복구된 손절로 **계획 없이 친 매매까지 R**을 채운다
  (계획 손절은 여전히 지어내지 않음 — 복구 못하면 R=null). 계획매매도 계획리스크 없을 때 거래소 손절로 폴백.
- **비트겟 페이지 청산내역 표** `components/ClosedTrades.tsx`: 종목·롱숏·진입가·청산가·손절가(복구)·순손익·진입/청산 시각,
  7/30/90일 토글, 순손익합계·승률·복구건수 요약. 가로 스크롤(모바일).
- 테스트 9건 추가(attachStops·exchangeRiskUsdt·reconcile 손절복구). 총 134 통과.

## 2026-09-16 (30차) — 매매일지: 심화 복기(진입·손절 타이밍 + 이벤트 대조 + 응대)

매매일지 성적탭에 매매별 **해부**를 추가. 코인 청산 매매를 Bitget 히스토리 캔들로 사후 분석.
- **타이밍 해부** `lib/tradeAutopsy.ts`: 손절 폭 vs ATR(타이트/적정/과대), 진입 위치(추격/눌림),
  진입 후 MAE/MFE(역행·순행 R) → 각 항목에 **응대방법**(손절 넓히고 수량↓·되돌림 대기·부분익절 트레일 등).
- **이벤트 대조** `lib/marketEvents.ts`: FOMC(2024~26 공식일정)·CPI(근사)·크립토 사건(ETF·반감기·GENIUS·
  CLARITY) 시드 + **사용자 커스텀 이벤트**(정확한 날짜 직접 등록, localStorage). 진입 ±3일 이벤트를
  '통과 전/후'로 표기하고, 고영향 이벤트 직전·직후·보유중 관통에 리스크 응대 제시. **방향 예측 아님(변동성 관점)**.
- `app/api/candles`(게이트 밖 공개 캔들) + `components/TradeAutopsy.tsx`(매매별 펼침, 이벤트칩은 즉시·타이밍은 온디맨드).
- 테스트 14건 추가(`tests/autopsy.test.ts`) — ATR·손절폭·진입위치·MAE/MFE·이벤트 대조 전부 고정. 총 62건.

## 2026-09-16 (29차) — 목표 수익률 시스템 (예측 0·산수 100) + 배포 복구

"월 5–10%"를 예측이 아니라 **기대값×빈도×리스크 역산**으로 접근하는 규율 시스템. 새 페이지 `/target`.
- **역산 엔진** `lib/targetPlan.ts`: 필요 회당 기대값(R)=목표%/(월매매수×회당리스크%), 실측 기대값·승률·W/L
  (계획 기록 매매만, 모르는 건 0으로 안 셈), 판정(unknown/negative/gap/on-track), **레버 4종**(승률·익절R·횟수·
  리스크 — 리스크 하드상한 2%), 이달 진행률(실현손익 입력 건만).
- **누수 엔진** `lib/leakage.ts`: 손절 초과·계획 없는 매매 손실·복구 매매(30분 근사)·수수료·펀딩을 USDT/시드%로.
- **규칙 강제**: 플래너가 서킷브레이커 작동·회당 리스크 2% 초과 시 저장 잠금(목표 계획 초과는 경고).
- **신호 실측 엣지**: 크론이 TP/SL로 판정한 `data` 브랜치 통계를 읽어 n≥30·기대값>0인 모드만 "채택 가능",
  그 신호만으로 목표 달성에 필요한 월 횟수 표기. 신호는 진입 후보일 뿐, 플래너→저널 경로만 목표 계산에 포함.
- **테스트 24건 추가**(`tests/target.test.ts`, `npm test`에 연결) — 역산·판정·레버·진행률·누수 전부 고정.
- **🐛 배포 복구**: 28차의 `app/favicon.ico`가 RGB PNG라 Turbopack "PNG is not in RGBA" 빌드 실패 → 그 뒤 모든
  Vercel 배포가 실패 중이었음(라이브가 옛 assetlinks·SW v1에 묶임). RGBA로 재생성해 복구.

## 2026-09-15 (28차) — APK 새 정체성 재빌드 (app.kospilab.twa) + 웹 탭 아이콘 수정

옛 패키지에 아이콘을 여러 번 갱신해도 폰 런처가 옛 파란 K를 계속 표시(재부팅·재설치 무효) →
**새 패키지 ID로 처음부터 재빌드**해 런처 캐시 문제를 원천 제거.
- **packageId `app.kospilab.twa`** v1.0.0(code 1), 같은 서명키. 산출 `Documents\KOSPILAB_v1.0.apk`(옛 파일명과 다르게).
  옛 `app.vercel.kospi_lab.twa`는 폐기 — assetlinks.json에서 제거, 새 패키지만 등록(전체화면 유지).
- **어댑티브 아이콘 배경**: bubblewrap 템플릿 흰색 → 네이비 `#05163f`(`values/ic_bg.xml`)로 패치, 흰 테두리 제거.
- **웹 탭 아이콘 미변경 수정**: `app/favicon.ico`(옛 파일)가 매니페스트보다 우선 → 새 아트워크로 재생성, SW 캐시 `kl-v2`.
- 빌드 폴더·옛 APK 전부 삭제 후 클린 재생성. APK 내부 아이콘 레이어를 직접 추출해 캔들차트 확인.

## 2026-09-15 (27차) — 앱 아이콘 교체 (캔들차트+상승화살표)

새 아트워크로 아이콘 교체 + 안드로이드 APK(TWA) v1.2.0 재빌드.
- 아이콘 가공(안드로이드 마스커블 기준): 흰 배경·코너 제거(근-흰색 픽셀→네이비 치환, 시안·레드 보존),
  네이비 `#05163f` 풀블리드 + 6% 세이프존 여백. `public/icon-512.png`·`icon-192.png` 교체(PWA 자동 반영).
- APK: 같은 서명키(`kospi-lab-twa.jks`)로 v1.2.0(code 3) 재빌드 → 기존 앱 위 덮어쓰기 설치. 스플래시 화이트 유지.

## 2026-09-14 (26차) — 배경 알림 (청산/손절 근접) 복구 + 강화

포지션 청산·손절 근접 시 앱을 꺼도 텔레그램으로 경보(크론 `coin-track.yml`, 15분).
- **🐛 치명 버그 수정**: `coinTrack.mts`의 `alerts.join('…')`에 raw 개행이 들어가 **스크립트 전체가
  파싱 단계에서 죽던 상태**(= 크론이 아예 안 돌던 것)를 `'\n\n'`로 수정. 로컬 실행 정상 확인.
- **강화**: 청산 위험 **2단계**(<15% 🛑 / <8% 🚨 임박, 별도 키로 재알림) + **손절 근접 사전경보**(손절선
  0.7% 이내 미도달 시 🔔) 추가. 기존 손절/목표 도달 알림 문구 정리.
- **UI 설명 카드**: 비트겟 페이지에 접이식 '배경 알림' 안내(동작·켜는 법: TELEGRAM·BITGET·SUPABASE 시크릿).
- 활성 조건: GitHub Secrets(TELEGRAM_BOT_TOKEN·CHAT_ID 필수, 포지션 감시는 BITGET, 손절/목표는 SUPABASE 동기화).

## 2026-09-14 (25차) — 주간 리뷰 (이번 주 vs 지난 주 · 규율 · 최다 실수)

매매일지 상단에 `WeeklyReview` 카드 추가(코인+주식 합산). 기록→개선 루프 요약.
- 월요일 기준 이번 주 지표 + **지난 주 대비 델타**: 마감·승률·기대값(R)·**계획 준수율**.
- **최다 실수 자동 추출**: 계획 없이 친 매매·미청산 방치·손절 초과 손실(≤−1.5R)·마이너스 기대값.
- **한 줄 총평**: 준수율/기대값에 따라 규율 진단. R은 계획 기록 매매만(‘모름’을 0으로 세지 않음).
- 실브라우저 검증: 지난주 대비 델타·최다 실수·총평 전부 정확.

## 2026-09-14 (24차) — 프리트레이드 플래너 (사이징·청산가·1R → 저널)

새 페이지 `/planner`. 진입 전에 규율을 먼저 세우는 도구(방향 아님).
- 입력: 종목·방향(롱/숏)·시드·1회 허용손실%·진입·손절·목표1/2·레버리지.
- 계산: **권장 수량**(손절 시 정확히 1R), 계획 노션·필요 증거금·1R·손절거리·**청산가 근사**·**손익비(R:R)**.
- 경고: 청산가가 손절보다 가까움(레버리지 과다)·증거금 시드 초과·방향 불일치.
- **저널 저장**: useCoinJournal로 '계획 매매' 기록 → 매매일지에서 실제 결과와 대조돼 승률·기대값(R) 환산.
- 메뉴 연결: 분석 그룹(모바일·데스크탑)·홈 분석 카드. **함정 수정**: Field 컴포넌트를 렌더 함수 밖으로
  (안에 두면 매 입력마다 리마운트→포커스 유실). 실브라우저 계산 검증(1R $10·노션 $333·청산 $80k·R:R 2R/4R).

## 2026-09-14 (23차) — 비트겟 페이지 라이트 테마 정리

옛 색상 클래스를 토스 토큰/대비 상향으로 교체(라이트에서 흐릿하던 것 해소).
- `bg-white/*` → `bg-[var(--surface-2)]`, `text-sky-400`·`bg-sky-500/60` → accent,
  손익 emerald/red-400 → -500, amber-400 → -600.

## 2026-09-14 (22차) — 메뉴 고도화: 검색·스와이프·데스크탑 아이콘·자주쓰는 자동화

옵션 4종 일괄 적용(빌드·tsc 0, 데스크탑 실렌더 + 팝업 정적 미리보기 검수).
- **③ 팝업 검색창**: 메뉴 상단 검색 입력 → 전체 항목 라벨 필터(결과 그리드/‘없음’). 지우기 버튼.
- **④ 시트 스와이프 닫기**: 바텀시트 헤더를 아래로 끌면 따라 내려가고 110px 이상이면 닫힘, 아니면 스냅백
  (touch 이벤트 + translateY, 드래그 중 transition 해제).
- **⑤ 데스크탑 알약 아이콘**: 대시보드·시장·내자산·분석·설계 알약에 라인 SVG 아이콘(모바일 세트와 통일).
- **⑥ 자주 쓰는 자동화**: 라우트 방문 빈도를 localStorage(`kl:visits`)에 누적 → 상위 4개를 자주쓰는으로,
  데이터 없으면 기본값(내주식·통합리스크·코인분석·매매일지)으로 폴백.

## 2026-09-14 (21차) — 메뉴 개선: 현재 위치 하이라이트 + 이용가이드 아이콘 통일

- **현재 위치 표시**: 팝업 메뉴(자주 쓰는 기능·전체 메뉴 그리드)에서 지금 보고 있는 페이지 타일을
  accent 하이라이트(소프트 배경·링·아이콘/라벨 컬러). HomeMenu에 usePathname/useSearchParams로
  정확 대조(쿼리 market까지). globals `.hm-item.on`/`.hm-quick a.on` 신설.
- **이용가이드 아이콘 통일**: 데스크탑 내비의 `📖` 이모지 → 라인 SVG(book-open)로 교체(아이콘 세트와 일치).
- 남은 옵션(미적용): 팝업 검색창·시트 스와이프 닫기·데스크탑 알약 아이콘·자주 쓰는 자동화.

## 2026-09-11 (20차) — 비트겟: USDT-M 선물 잔액 우선 표시

현물(spot)만 크게 보이던 것을 실제 자금이 있는 선물 중심으로 재구성(빌드·tsc 0).
- **USDT-M 선물 잔액 카드 신설**: positions 라우트가 이미 받아오던 `account`(순자산 equity·
  사용가능·미실현손익·증거금코인 = `/api/v2/mix/account/accounts?productType=USDT-FUTURES`)를
  상단에 크게 표시(accent 토큰). 기존엔 포지션 있을 때만 미실현손익이 작게 나왔음.
- **현물(spot)은 보조로 강등**: 총 평가금액 히어로 → 소형 카드(잔액>0일 때만). 빈 목록 문구도 '현물' 명시.
- 서버 변경 없음(UI만) — 선물 read 권한 없으면 기존 안내 그대로.

## 2026-09-11 (19차) — 전체 메뉴를 홈에서 팝업(바텀시트)으로 이동

홈 본문을 시세 중심으로 비우고 메뉴를 버튼 → 팝업으로 옮김(빌드·tsc 0, 정적 미리보기 검수).
- **홈 본문에서 전체 메뉴 제거**: page.tsx에서 `<HomeMenu/>` 삭제 → 모바일 홈은 주식·코인 시장환경·
  현물 ETF·관심종목만. 라이브 시세를 스크롤 없이 바로 봄.
- **메뉴 버튼 → 바텀시트 팝업**: NavTabs 모바일을 인라인 아코디언에서 **버튼(알약) + 바텀시트 모달**로 교체.
  그랩 핸들·타이틀·닫기 X·딤 배경(backdrop-blur)·슬라이드업 애니메이션, 열림 중 배경 스크롤 잠금.
  팝업 안에 대시보드 홈 + 자주 쓰는 기능 + 전체 메뉴(라인 SVG 그리드)를 렌더.
- HomeMenu를 팝업용으로 리팩터(onNavigate로 항목 탭 시 닫힘, 바깥 카드 제거). globals: `.mfade`/`.msheet` 키프레임.

## 2026-09-10 (18차) — 모바일 홈 전면 리디자인: 라인 SVG 아이콘 + 새 구성

이모지 타일 벽을 걷어내고 모바일 홈을 처음부터 다시 설계(빌드·tsc 0, 실브라우저 검수).
- **아이콘 전면 교체**: 24종 이모지 → **일관된 스트로크 라인 SVG 아이콘 세트**(HomeMenu 내 `ICON` 사전,
  viewBox24·strokeWidth 1.7·round). 원은 arc 서브패스로 표현해 path 하나로 유지. 코인 ₿는 정식 심볼로 정렬.
- **레이아웃 새 구성**: ① **자주 쓰는 기능** 밴드 신설 — 이 앱의 본질(손절·사이징·기록) 도구 4종
  (내 주식·통합 리스크·코인분석·매매일지)을 컬러 칩 카드로 상단 노출. ② **전체 메뉴**는 중성 타일(surface-2)
  + 그룹별 컬러 아이콘(시장=블루·자산=그린·분석=바이올렛·설계=앰버)으로 정돈. 라이트·다크 토큰 자동.
- globals.css: `.hm-quick`·`.hm-grid`·`.hm-ic`·`.c-*`/`.qc-*` 스타일 신설(이전 `.home-*`/`.tint-*` 대체).

## 2026-09-10 (17차) — 모바일 마감: 홈 런처 타일 + 히어로 지수 정렬

토스 리스킨을 모바일까지 일관되게 마무리(빌드·tsc 0).
- **홈 런처 타일**: 옛 생색(sky/emerald/violet/amber Tailwind) → 토스 소프트 틴트 토큰(`.tint-a~d`)으로
  교체해 데스크탑 팔레트와 통일. 테두리·호버리프트 제거하고 58px 소프트 타일 + 눌림(active scale) 피드백,
  `.home-grid` 4열 간격 정리. 라이트·다크 자동 대응(토큰 기반).
- **히어로 지수(모바일)**: 코스닥·코스피200·나스닥 `.stat`이 좁은 폰에서 `min-width:120px`로 들쭉날쭉
  줄바꿈되던 것을 640px 이하에서 **3등분 그리드**로 균등 정렬. 히어로 여백·수치 크기도 모바일 축소.

## 2026-09-10 (16차) — 토스 구조 이식: 알약 내비 + 히어로 카드

토큰에 이어 myzipplan의 구조까지 "똑같이" 맞춤(빌드·tsc 0).
- **알약 세그먼트 내비**: 데스크탑 상단 탭을 `.topnav` 알약바 + `.navlink.active`(흰 알약+섀도우)로 교체.
  드롭다운을 없애고 그룹 선택 시 하위 메뉴를 `.chip-row` 서브칩으로 노출(myzipplan 2행 탭 방식). 모바일 아코디언 유지.
- **히어로 카드**: 대시보드 주식 지수를 `.hero`(그라디언트 표면·거대 수치)로 재구성 — 코스피 대형 수치 +
  상승/하락 상태 pill(한국 관행 상승=빨강·하락=파랑) + 코스닥·코스피200·나스닥 `.stat` 타일. 기존 IndexCards 대체.
- 잔여 sky 하드코딩 → accent 토큰 통일(홈 링크·관심종목 호버). 라이트/다크 양쪽 정상.

## 2026-09-10 (15차) — 디자인 3차: 토스(Toss) 스타일 이식

myzipplan.com의 토스 계열 디자인 시스템을 분석해 전체 이식(빌드·tsc 0).
- **토큰 교체**: globals.css `:root`/`html.dark`를 토스 팔레트로 재정의하고 KOSPI 변수명에
  매핑 → 27개 페이지 무수정으로 전체 리스킨. 배경 `#f2f4f6`, 카드 순백, 액센트 **토스블루 `#3182f6`**,
  잉크 `#191f28`, 시맨틱(ok/warn/amber)·라인·섀도우 3단·라운드(22/16/12) 전부 토스 값.
- **타이포**: Pretendard 유지, 본문 letter-spacing −0.01em·line-height 1.5·제목 −0.02em(토스 시그니처).
- **표면**: 상단 글로우·그레인 제거해 평평한 클린 룩, `.surface` 무광택화, `.kl-cta` 솔리드 프라이머리,
  `rounded-2xl→22px`·`rounded-xl→16px` 상향, 스크롤바·포커스·선택색 토스 톤.
- **헤더/네비**: 로고 타일·워드마크 토스블루, 네비 활성/드롭다운 accent 토큰 통일(sky 하드코딩 제거).
- 라이트 기본·다크 토글 양쪽 정상, 컴포넌트 변수명 전부 보존해 회귀 없음.

## 2026-08-24 (14차) — 디자인 2차: 표면 크래프트

토큰 기반 위에 고가시성 표면을 다듬음(프로덕션 실측·콘솔 에러 0).
- **내비**: 활성 탭 accent 언더라인+텍스트, 드롭다운 surface+accent 톤 통일(sky 하드코딩 제거).
- **홈**: 주식/코인 섹션 제목에 accent eyebrow 바, QuickCard·관심종목·홈메뉴 타일에 표면 광택+호버 리프트.
- **코인분석 미터**: 방향(초록)·진입적합(하늘→남색)·신뢰도(보라→자홍)·이벤트위험(호박→빨강) rounded-full 그라디언트.
- **질감·발광**: 배경 미세 그레인(z-index -1·클릭 통과), 개장 상태점 발광(dot-live).
- 유틸 신설: hover-lift·surface·eyebrow·kl-cta·dot-live(재사용). 라이트/다크 양쪽 정상.

## 2026-08-24 (13차) — 전역 디자인 정비

- **폰트**: Pretendard(동적 서브셋 CDN) 로드 — 한글 타이포 대폭 개선(프로덕션 실측 `fonts.check`=true).
  숫자 tnum 고정·제목 letter-spacing·antialias. 오프라인/실패 시 시스템 폴백.
- **토큰 버그 수정**: `--accent`·`--card`가 선언 없이 쓰여 일부 UI가 무색이던 문제 수정 →
  코인분석 '진입적합' 막대·모드 탭이 이제 accent 색으로 보인다(브라우저 실측).
- **가독성·깊이감**: 다크 `--text-muted` 대비 상향(#6b7280→#8b93a7, 830곳)·bg 계층 정비·상단 광원
  그라디언트·다크에도 은은한 그림자. 포커스 링(accent)·선택색·스크롤바·전이 통일.
- **헤더**: 그라디언트 K 마크 + 워드마크, 부제 '투자 리스크 관리'.
- 다크·라이트 양쪽 프로덕션 실측(회귀 0, 콘솔 에러 0). 토큰 이름 전부 보존해 안전.

## 2026-08-24 (12차) — 발굴 기능 4종(복기·서킷브레이커·감시·AI코치)

실제 데이터(-419·계획0건·5연속손절)가 방향을 정해줬다. "다음에 뭘 사라"가 아니라
"왜 지는지 보여주고 다음 손실을 막는" 쪽 — 예측이 아니라 과거 사실·산수라 정직하다.

### A. 매매 복기 (`lib/journalRetro`·순수함수·테스트 5)
청산 매매를 계획 유무·방향·시간대·요일·연속손절로 쪼갠다. 자동 유도 통찰(계획 없는 매매가
손실 주범/틸트/방향 편향/최악 시간대). 브라우저 실측: "21건 중 21건(100%) 계획 없이 침 -419",
"최대 8연속 손절", 롱 33%/-361·숏 25%/-58 — 손실 기전이 그대로 드러남. `components/RetroReport`→/journal.

### B. 손실 서킷브레이커 (`lib/circuitBreaker`·순수함수·테스트 6)
연속손절·일/주 실현손실 한도로 ok/warn/blocked. blocked 면 실행가능 판정을 NO 로 만든다.
한도는 사용자 설정(`useRiskLimits`, 동기화됨). 실측: "5연속 손절 → 오늘 진입 금지" 정상 작동.
`components/CircuitBreakerBar`→/journal·/coin-analysis 상단.

### C. 포지션 감시 + 텔레그램 (`scripts/coinTrack.mts` 접붙임)
기존 15분 크론에 통합(별도 크론=data 브랜치 push 경쟁). 선물 키 있을 때만: 청산<15% + 동기화된
계획 손절/목표 도달 → 텔레그램. 6h 재발화 억제(db.watch). ⚠ GitHub Secrets(TELEGRAM·BITGET·SUPABASE)
등록해야 실동작 — 미등록이면 조용히 skip.

### D. AI 복기 코치 (`app/api/coach`·게이트)
통계+브레이커를 Claude 에 넘겨 행동 피드백. **방향·종목 추천 프롬프트에서 강력 금지**(엔진이 방향을
못 맞히는 게 측정됨). `components/AiCoach`→/journal. ⚠ 코드·게이트 정상이나 프로덕션
`ANTHROPIC_API_KEY`가 무효(401) — 기존 AI 브리핑도 같은 키라 함께 먹통. **사용자: 유효 키로 교체 필요.**

- 테스트 75 → **87개**. tsc 0·build OK.

## 2026-08-24 (11차) — PWA 설치 지원

manifest 는 이미 완비돼 있었고(standalone·아이콘 192/512 maskable) 서비스워커만 없었다.
`public/sw.js` + `components/PwaRegister.tsx` 추가로 **설치형 PWA** 완성 — 안드로이드 WebAPK 설치,
오프라인 실행, 설치 배너가 켜진다. SW 전략은 network-first(온라인이면 항상 최신 → 구버전 갇힘 없음,
오프라인이면 캐시 폴백). 내부 API·외부 시세는 캐시하지 않는다(낡은 데이터 방지).
프로덕션 실측: SW `activated`·controller true·scope 루트, 콘솔 에러 0.

## 2026-08-24 (10차) — "사도 되는가"에 정직하게 답하기 + 브라우저 실측

### 🎯 실행 가능 판정 (Go / No-Go) — `lib/tradeGate.ts` · `components/TradeGate.tsx`
사용자 제안("엔진 신호를 '사도 되는가'로 바꾸면 완성도가 오르지 않나")에서 출발했다.
그대로 하면 측정 결과와 정면 충돌한다 — 이 앱 엔진은 **방향을 못 맞힌다**(코인 49.7%·41.7%,
주식 54.1%인데 대조군 54.8%). 그래서 질문을 바꿨다:
**"오를까?"가 아니라 "내가 고른 이 방향으로 이 크기로 걸어도 감당되나?"** — 이건 예측이 아니라
계좌 상태와 산수라서 **단언할 수 있다.**
- 7개 검사: 손절 정의 · 계좌 대비 손실(2%) · 청산 여유(2배) · 증거금 실행가능 · 손익비(1) ·
  방향 쏠림(70%) · 이벤트(12h) → **GO / RESIZE / NO**
- 실패마다 **무엇을 바꾸면 되는지** 제시(최대 배율·최대 노션·허용손실 상한). 판정만 하고
  길을 안 알려주면 쓸모가 없다
- 계좌를 못 읽으면 통과가 아니라 **unknown** — 모르는 걸 통과로 위장하지 않는다
- 화면에서 **"GO는 오른다가 아니라 틀려도 버틴다는 뜻"**을 반복 명시(초록 배지를 매수 신호로
  읽는 순간 이 도구는 해로워진다). 배지도 초록 대신 sky 톤
- `/coin-analysis` 리스크 패널에 연결 — 사이징을 정한 직후가 "해도 되나"를 물을 자리다

### 🐛 기대값이 손실을 본전으로 둔갑시키던 문제 (브라우저 실측에서 발견)
거래소 자동수집 21건(실현 **−419 USDT**)이 반영된 상태에서 **기대값이 `+0.00R`로 표시**됐다.
`avgR: (resultR ?? 0)` 이라 **"R을 모른다"가 "R이 0이다"로 둔갑**한 것 — 실패한 계좌가 본전으로 보였다.
- `journalStats`: avgR 을 R 이 기록된 건만으로 계산, `rCount`/`noRCount` 노출
- `/journal`: "R 환산 불가 — 계획 기록 없음" 명시 +
  **"기대값이 비고 실현손익이 마이너스면 계획 없이 친 매매가 손실을 냈다는 뜻"** 이라고 설명

### 🛡 동기화 데이터 손실 경로 차단 (실사용 직전 발견)
두 기기에 서로 다른 기록이 있을 때 **나중에 연 기기가 먼저 기기의 매매일지를 조용히 덮어썼다.**
- **빈 값은 업로드하지 않는다** — 새 기기에서 앱을 한 번 여는 것만으로 다른 기기 기록이
  지워지는 사고 차단(대가: 마지막 한 건까지 지운 것은 전파 안 됨 — 되살아나는 불편은 되돌릴 수
  있지만 지워진 기록은 되돌릴 수 없다)
- **첫 동기화 충돌 시 자동 진행 중단** → 표시등 주황 `확인 필요` + "이 기기 것으로 / 클라우드 것으로"

### 🔍 브라우저 실측 (프로덕션)
`/risk` 렌더링 · 동기화 표시등 `동기화됨`(기기 PC-ssga) · `/journal` 21건 반영(승률 28.6%,
−419 USDT) · 3모드 정직성 문구 · **콘솔 에러 0** · 실행 가능 판정 7개 검사 전부 정상 표시.
- 실측에서 발견: 포지션이 없는데 "방향 쏠림 100%"가 통과로 보이던 표현 → "첫 포지션"으로 수정
- 테스트 63 → **75개**

## 2026-08-24 (9차) — 대규모 업그레이드 4종

### 🔬 주식 룰 엔진 엣지 측정 → 코인과 같은 결론(우위 없음)
코인만 측정하고 주식은 한 번도 대규모로 재지 않았던 공백을 메웠다(`scripts/backtest-stock-lab.ts`).
32종목·3년(2023-07~2026-08)·기술적 신호만(과거 수급 재현 불가).
- **엔진 362건 승률 54.1%(±2.6%p) · 기대값 +0.083R · 수수료·세금 반영 −0.017R**
- **대조군(진입 필터만 제거, 손절·목표·보유 동일) 3,441건 54.8%** → **엔진 − 대조군 = −0.7%p(z=−0.25)**
  → 진입 판정이 값어치를 못 한다. 54%라는 승률은 엔진 실력이 아니라 **롱온리 상승장 베타**
- 점수 변별력도 없음: 신호 97%가 30~50 구간에 몰리고 50~70 구간(n=9)은 오히려 44.4%
- ⚠ 발견: 네이버 fchart `count=` 파라미터는 헤더행만 반환한다 → `startTime`/`endTime` 필수.
  `lib/naver.ts`의 `fetchChartData`가 이 깨진 방식이었고 **호출처가 없어 아무도 몰랐다**(수정)
- UI 반영: `/stock-analysis` 성적표 경고를 "미검증"→"측정됨·대조군보다 낮음"으로, 배지를
  `체크리스트 통과 (우위 아님)`으로, 전역 푸터·홈 안내에 주식 수치 추가

### ☁️ 클라우드 동기화 — PC↔모바일 자동 동기화
11개 localStorage 자산(매매일지·후보·관심종목·포트폴리오·가상투자·알림)이 기기에 갇혀 있어
PC에서 적은 매매를 폰에서 볼 수 없던 구조적 한계를 해소.
- `lib/cloudSync.ts` — **기존 훅 11개를 전혀 건드리지 않는다.** 바깥에서 스냅샷 해시를 관찰해
  변경을 감지하고, 키 단위 Last-Write-Wins 로 병합. 배열 병합 대신 LWW 인 이유는
  삭제와 미수신을 구별할 수 없어 지운 항목이 되살아나기 때문
- `app/api/sync/route.ts` — Supabase REST(의존성 0). **한 번의 POST 로 push+pull 동시 처리**.
  env 미구성 시 `{configured:false}` 200 → 조용히 로컬 전용으로 동작(에러 아님)
- `hooks/useCloudSync.ts` — 4초 폴링 변경감지 + 2.5초 디바운스 push + 90초 pull + 탭 복귀 시 pull.
  내려받은 변경이 있으면 첫 동기화에서 1회만 새로고침(세션 플래그로 루프 차단)
- `components/SyncIndicator.tsx` — 헤더 표시등. 미구성·잠금·실패를 **각각 다른 색과 문구로 노출**
- `supabase/migrations/2026-08-24-cloud-sync.sql` — `kl_sync` 테이블(공용 프로젝트라 kl_ 접두사),
  RLS 켜고 정책 없음 = service_role 외 전면 차단. 라우트는 게이트(`/api/sync`) 뒤

### 📓 포지션 자동 기록 — 성적표의 생존 편향 차단
손으로 적는 매매일지는 이긴 매매만 남기 쉬워 성적표 자체가 거짓말이 된다. 거래소가 아는
사실(실현손익·수수료·펀딩)로 자동으로 채운다.
- `app/api/bitget/history/route.ts` — 선물 청산 이력(`history-position`), 읽기 전용·게이트 뒤
- `lib/bitgetJournal.ts` — **순수 함수 대조 로직**(테스트 11개로 고정): 계획 기록에 실제 손익을
  채워 닫고, **계획 없이 친 매매는 숨기지 않고 새 기록으로 남긴다**("계획 매매 vs 충동 매매"
  성적 차이가 보여야 저널이 거울 노릇을 한다). 계획 리스크를 모르면 R 을 지어내지 않고 null
- `components/ExchangeReconcile.tsx` + `/journal` 연결. 중복 반영 방지(positionId 표식)

### 🛡 통합 리스크 대시보드 `/risk`
다른 화면이 "이 매매 하나"를 본다면 여기는 계좌 전체를 합쳐서 본다.
- `lib/riskDashboard.ts` — 순수 함수(테스트 8개): 총 익스포저·실효 레버리지·동시 손절 시 손실·
  방향 편중·최대 집중도·청산 최근접. 원화 환산 합산(환율 함께 반환해 검증 가능)
- **숫자에서 자동 유도되는 경고**: 청산 8%/15% 임박, 실효 레버리지 3배/6배 초과, 단일 종목 40%/60%,
  방향 편중 80%, 그리고 **"코인은 나눠 담아도 분산이 아니다"**(상관 0.8~0.95) 경고
- 손절 계획이 없는 열린 기록은 합계에서 빼고 **"계산되지 않은 위험"으로 별도 표시**(조용히 0 처리 금지)
- 테스트 42 → **61개**

## 2026-08-22 (8차)

### 🔬 성장주 발굴 "고칠 것 0" 심층 정리 — 검증·수정·구조 개선 일괄
2차 완성도 점검의 잔여 3건을 심층 분석 후 전부 해소. 분석 과정에서 숨은 결함 2건 추가 발견·수정.

**검증(수정 불필요로 판명)**
- 미국 스캔 신선도: COIN 스캔가 186.49 == Yahoo 실시간 chart 186.49 완전 일치, 1h TTL 갱신 실측
- 모바일 390px: flex-wrap ×8·반응형 그리드·고정폭은 결과표 `min-w-[880px]` 하나뿐이며
  `overflow-x-auto` 내부(의도된 내부 스크롤) — 코드 레벨 무결

**🐛 추가 발견·수정 (`app/growth/page.tsx`)**
- **한글 검색어가 US 탭에서 Yahoo로 라우팅** → '삼성전자'가 005930.KS(스캔 불가 형식)로 나와
  선택 시 실패. 한글은 어느 탭이든 국내 검색으로
- **영문 질의의 국내 상장 반환**('samsung'→005930.KS)도 서버 정규식(숫자 불허)에서 실패
  → scanOne에서 `^\d{6}\.K[SQ]$` 패턴을 국내 코드로 정규화
- **map 안 프래그먼트 key 누락**(React 리스트 경고) → `<Fragment key={r.code}>`
- **US 컬럼 라벨 정합**: US는 Yahoo 특성상 '매출YoY·분기'(revenueGrowth=분기)·'순익YoY·분기'
  (earningsGrowth=순이익, 영업이익 아님) — 라벨·툴팁으로 명시. 값은 정확, 의미 표기만 교정

### 🏗 크론 스냅샷을 data 브랜치로 이전 — Vercel 배포예산 근본 해결
15분 크론이 main에 커밋할 때마다 Vercel이 배포를 만들어 **하루 96배포 = 무료 한도(100)의 96%**를
낭비했다(2026-08-21 실제 차단 원인, [skip ci]는 Vercel에 무효). `data/coin-signals.json`은 앱 코드가
전혀 읽지 않으므로(소비처는 크론 스크립트 자신뿐) 격리에 기능 손실 0.
- `coin-track.yml`: origin/data에서 이전 스냅샷 복원 → 실행 → **plumbing(commit-tree)으로 data
  브랜치에만 커밋**(워킹트리 전환·main 접촉 없음, 변경 없으면 push 생략)
- data 브랜치 시드(`64978c9`, 미청산 포지션 연속성 보존) · main에서 `git rm --cached` + .gitignore
- 부수 효과: main 히스토리에서 하루 96개 봇 커밋 소멸, §9 "push 전 rebase" 강박 해소

## 2026-08-21 (7차)

### 🐛 성장주 발굴 — 미국 종목 검색이 탭에 막히던 문제
'한국 전체'(기본) 탭에서 영문명(예: coinbase)을 검색하면 국내(KRX) 검색으로 라우팅돼 결과가
안 나왔다. API·스캔 자체는 정상이었고 검색어→시장 라우팅이 탭에만 묶여 있던 게 원인
(`app/growth/page.tsx`, 브라우저 실측으로 재현·확인):
- 검색 대상 시장을 **탭이 아니라 검색어로 판정**: 6자리 숫자=국내코드, 영문명·티커=미국, 한글=현재 탭.
  → 'coinbase'는 어느 탭에서든 COIN을 찾는다
- 검색 결과 선택 시 **그 종목의 시장으로 탭 자동 전환**(현재가·시총·컬럼 표기 일치). scanOne이
  탭이 아니라 선택 종목(`hit.us`)을 따르도록 변경
- 힌트에 "영문명·티커(예: coinbase, COIN)는 어느 탭에서든 미국 종목을 찾습니다" 추가
프로덕션 실측: `/api/overseas/search?q=coinbase`→COIN, `/api/growth-scan?tickers=COIN`→정상 스캔.

## 2026-08-21 (6차)

### ✨ 모바일 홈메뉴 — 앱 런처형 아이콘 그리드(아이쑥쑥·약보듬 방식)
모바일 첫 화면에서 전체 기능을 아이콘 타일로 한눈에 탭하도록 홈메뉴를 추가.
- `components/HomeMenu.tsx` 신규 — NavTabs 그룹(시장·내자산·분석·설계 22개 라우트+이용가이드)을
  4열 아이콘 타일 그리드로. 다크 테마 그룹별 색조(sky/emerald/violet/amber)
- `app/page.tsx` — 모바일(`md:hidden`) 최상단에 HomeMenu 배치. 기존 하단 QuickCard 4섹션은
  데스크탑 전용(`hidden md:block`)으로 돌려 모바일 중복 제거. '이 도구의 역할' 정직성 문구는 양쪽 유지
- `components/Header.tsx` — 로고를 홈('/') 링크로(모바일에서 홈메뉴 복귀 경로)
- 데스크탑은 기존 상단 탭 드롭다운 내비 그대로. 게이트: 테스트 42/42·tsc 0·build OK

## 2026-08-21 (5차)

### 🔬 3모드 진입엔진 엣지 측정 → 우위 없음(옛 엔진과 동일 결론)
coin-signal에서 이관한 3모드 엔진(`lib/coinSignalModes.ts`)은 이관 이후 한 번도 백테스트를
거치지 않았다("정교해 보임 ≠ 측정된 우위"). 라이브 경량 라우트(`/api/coin-signal`)와 동일하게
캔들만으로 `buildModes`를 과거에 돌리는 측정 스크립트를 새로 만들어(`scripts/backtest-modes.ts`,
무lookahead 완결봉·대칭1R 승률·수수료 0.3R 차감) 45일·4코인으로 쟀다.

- **전체 81신호·72청산 · 승률 41.7%(±5.9%p) · 기대값 −0.167R(대칭1R) / −0.530R(엔진RR·수수료)**
- 손익분기 50% 미달(z=−1.41), 수수료 반영 시 ~65% 필요. LONG 40%·SHORT 42.9% 둘 다 마이너스
- 모드별 scalp 46.6%(n=58) · swing 27.3%(n=11) · position 0%(n=3). EQ 80~100만 66.7%지만 n=6이라
  근거 불가(과적합 함정). → **옛 룰엔진(49.7%)과 같은 결론: 측정 가능한 엣지 없음**

### ✨ 3모드 UI를 리스크 재정의와 정렬 (엔진 통일 방향 (b) 확정)
측정 결과에 따라 "정본화(a)"가 아니라 "리스크 프레이밍 통일(b)"로 결정. 3모드 화면이 옛 엔진과 달리
`TRADE=진입`(초록불)·`ULTRA=최상급`으로 진입 신호처럼 보이던 것을 옛 엔진과 같은 정직성 기준으로 맞춤
(`app/coin-analysis/page.tsx` ModesSection):
- `TRADE` 배지: 초록불(`bg-emerald-500`) → 옛 `entryOk` 배지와 같은 톤(sky), 문구 `진입`→`체크리스트 통과`
- `NO_TRADE` 문구 `거래거부`→`조건 미달`, `ULTRA 최상급`→`ULTRA · 다수조건 충족(우위 아님)`
- 섹션에 정직성 문구 추가 — "TRADE·ULTRA는 진입 신호가 아니라 체크리스트 충족도. 자체 측정 41.7%로 우위 미확인"
- 두 엔진은 삭제하지 않고 상보적 뷰로 유지(둘 다 무엣지). 코드 레벨 은퇴는 사용자 결정 대기

### 참고
- 게이트 통과: 테스트 42/42 · tsc 0 · build OK. 측정 스크립트는 앱 배포와 무관(scripts/ 는 tsconfig exclude)

## 2026-08-07 (3차)

### 🚀 배포
- 프로덕션 배포 (`ce92f58`~`4906c2b`) — 리스크 관리 기능 5종

### ✨ 추가
- **매매일지 성적표 `/journal`** — 진입 엣지가 없다는 걸 확인한 이상 정직한 핵심 가치는
  "엔진이 뭐라 하든 내가 실제로 얼마나 버는가". 그동안 저널이 분석 결과 안에 묻혀 있어
  포지션 확인하려면 30초짜리 분석을 다시 돌려야 했다. 독립 페이지로 빼고 **시간창별(7/30일/전체)
  승률·기대값·R 분포·미실현손익·미청산 비율(규율 신호)**을 보여준다 (`lib/journalStats.ts` 순수 함수, 테스트로 고정)
- **Bitget 선물 포지션 연동** — 기존엔 현물만 봤다. `mix` API로 열린 포지션의 실제 평단·마크가·
  레버리지·미실현손익·**청산가와 청산까지 거리(%)**를 표시하고, 15% 미만이면 붉은 경고. 읽기 전용.
  (`app/api/bitget/positions`, `/bitget` 페이지). ⚠ 현재 사용자 키가 현물 전용이라 **선물 읽기 권한 추가 필요**(화면 안내)

### 🐛 수정
- **숏 손절률이 "−"로 표시되던 문제(M-2)** — 손절 부호가 방향과 무관하게 하드코딩. 숏은 손절이
  진입가 위이므로 `+`. 리스크 도구에서 손절 방향 오독은 실수를 유발한다
- **저널 레버리지가 실제 사용값이 아니던 문제** — 엔진 권장값(conservative)을 저장했다.
  리스크 패널 슬라이더로 실제 설정한 배율·시드·노션을 기록하도록 수정. "엔진 판정 vs 내 실제 성적" 비교의 전제 복구
- **저널 100건 상한 → 1000** — 성적 실측이 핵심인데 하루 3건이면 한 달 뒤부터 오래된 기록이 무통보 삭제됐다
- **M-5 펀딩 회피가 정산 직후 통과** — `minToFunding >= 0`이라 정산 직후(음수)면 게이트가 열렸다. `>= -5`로
- **M-4 confidence가 차단 게이트 미반영** — 이벤트 임박·펀딩·진입자리 불량인데 "🟢 견고 95%"가 떴다. hardBlock 시 상한 40·"약함" 강제
- **M-1 수급이 전부 0인데 통과** — `dataDays` 추가, 실제 데이터 3일 미만이거나 5일 합계가 전부 0이면 검증됨으로 안 봄

### 참고
- 테스트 35 → 42개. Bitget 선물은 로컬에서 서명 조회를 검증할 수 없어 배포 후 프로덕션 실측(라우트·서명 정상, 키 권한만 부족 확인)

## 2026-08-07 (2차)

### 🚀 배포
- 프로덕션 배포 (`26450a6`, `1054f57`) — 펀딩 전략 본검증 + 도구 위치 재정의

### 🔬 펀딩 전략 본검증 → 탈락
1차 실험에서 유망해 보인 펀딩 극단 되돌림을 19종목·2,168건·6단계로 검증했더니 **3/6 통과**로 떨어졌다.
결정타는 대조군 — **펀딩과 무관하게 그냥 항상 숏을 쳐도 +0.222%(t=5.02)** 가 나오는 기간이었다.
전략의 83%가 숏 진입이라 수익 상당분이 **하락장 베타**였고, 롱 방향은 음수(−0.228%),
후반 45일에선 효과가 6배 약화됐다. 파라미터도 민감해서 16h/2%에선 2/6만 통과한다. → **실투자 금지**
- 판정 기준 자체도 강화했다: V2를 "양수"에서 "유의"로, 대칭성(V5)·기간편향 대조군(V6)을 판정에 포함.
  느슨한 기준으로 "통과"가 나오는 게 가장 위험하기 때문 (`scripts/validate-funding.ts`)

### 🔄 변경 — 도구 위치를 리스크 관리로 확정
측정 결과 어디에도 엣지가 없어, 앱이 스스로를 소개하는 문구를 전면 정리했다.
- 내비: `수급·추세·재무 매수판단` → `체크리스트`, `BTC·ETH·XRP·SOL 롱숏 판단` → `손절·사이징·리스크 점검`
- 홈 첫 화면에 **"이 도구의 역할"** 안내 — 방향 판단은 사용자 몫, 앱은 손절·사이징·청산가·기록 담당
- 사이트 제목 `주식·코인 투자 분석` → **`투자 리스크 관리 대시보드`**, 설명에 "매매 신호를 제공하지 않습니다"
- 전역 푸터·코인 분석 상단에 백테스트 승률 49.7%·예측 우위 미확인 고지
- **AI 브리핑 프롬프트 수정(가장 직접적인 문제였다)** — 코인 `【진입 관점】 롱/숏/관망`,
  주식 `【종합 판단】 매수우위/중립/비중축소`를 둘 다 **`【리스크 점검】`**(무효화 조건·손절 위치·최악 시나리오)로 교체.
  "진입을 추천하지 말 것" 규칙을 프롬프트에 명시. 배포본 실측에서 추천성 표현 0건 확인
- `매수우위/중립/비중축소` 라벨 자체는 유지 — 룰 엔진 점수를 서술하는 어휘이고 매매일지·포트폴리오
  배지에 저장·사용되므로, 라벨을 바꾸는 대신 배너·툴팁으로 맥락을 잡는 편이 부작용이 적다

## 2026-08-07

### 🚀 배포
- 프로덕션 배포 (`fbb3b4b`, `fb0221e`, `0cc80cf`) — 정확도 결함 수정 + 엣지 측정 + UI 정직성

### 🐛 수정 (정확도)
- **백테스트가 미래를 보고 있던 문제** — `sliceUpTo`가 봉의 '시작'시각으로 잘라, 5분봉 종가로
  판단하는 시점에 1H봉이 **평균 22.6분(최대 45분) 미래**를 포함했다. 1H는 가중이 가장 큰
  타임프레임이라 성적표가 통째로 부풀려졌다 — 실측 **승률 +4.0%p 과대**. 완결봉만 쓰도록 수정
- **VWAP이 백테스트에서 사실상 꺼져 있던 문제** — 기준일을 서버 현재시각으로 잡아 과거 구간에선
  항상 null. **표본의 81%에서 VWAP ±6점 미적용** → 백테스트와 실전이 다른 룰을 돌리고 있었다
- **EMA가 EMA가 아니던 문제** — 시드를 계산해놓고 첫 루프에서 덮어써 죽어 있었다. ema200에
  첫 종가가 **13.67% 가중**으로 남아 추세 지속 방향으로 편향. 워밍업 SMA + EMA 재귀로 수정
- 백테스트가 라이브의 상위TF 레짐 필터를 빼고 돌던 것을 연결 — **서로 다른 엔진을 재고 있었다**

### 🔬 측정 (새로 만든 것)
- `scripts/backtest-lab.ts` — Bitget 히스토리 페이지네이션으로 대규모 표본 확보.
  기존 프로덕션 백테스트는 조회 한도(1000봉=83시간)에 묶여 표본이 40건대라 통계적으로 무의미했다
- `scripts/backtest-deriv.ts` — 파생 수급 레이어 A/B. "파생은 스냅샷이라 백테스트 불가"라는
  기존 전제가 틀렸음을 확인(공개 히스토리 API로 재현 가능)
- `scripts/backtest-funding.ts` — 펀딩 극단 되돌림 전략
- `scripts/measure-backtest-bias.ts` — 미래참조량 정량화

### ⚠️ 중요 — 엔진 엣지 측정 결과
가격 기술적 지표만: 45일·727건 **승률 49.7%**. 파생 포함: 28일·407건 **48.4%**.
둘 다 오차범위 안이라 **동전 던지기와 구분되지 않는다.** 수수료를 감안하면 확실히 마이너스.
**펀딩 극단 되돌림**은 1차 실험(4코인·n=196)에서 16h 보유 +0.461%(t=3.62)로 유망해 보였으나,
**19종목·6단계 본검증에서 탈락**했다(`scripts/validate-funding.ts`, 3/6 통과).
결정적 이유는 대조군 — **펀딩과 무관하게 그냥 항상 숏을 쳐도 +0.222%(t=5.02)** 가 나오는 기간이었다.
전략의 83%가 숏 진입이라 수익의 상당 부분이 펀딩 엣지가 아니라 **하락장 베타**였고,
롱 방향은 오히려 음수(-0.228%), 후반 45일에선 효과가 6배 약화됐다.
→ **실투자 금지.** 상세는 PROJECT_STATUS §0

### 🔒 UI 정직성
- 코인·주식 분석의 백테스트 카드에 **"엣지가 검증되지 않았습니다"** 배너 + 측정 수치 명시
- `entryOk` 배지를 초록 `✓ 진입 조건 충족` → 파랑 **`체크리스트 통과 (우위 아님)`**로 변경.
  초록불+체크표시가 "사도 된다"로 읽히던 문제. 실투자 중이라 정직성 문제로 취급
- 주식은 대규모 측정을 안 했으므로 코인 수치를 빌려 쓰지 않고 범위를 정확히 한정

## 2026-08-06 (5차)

### 🚀 배포
- 프로덕션 배포 (`ddcd38a`, `1b08f3b`) — 후보 파이프라인

### ✨ 추가
- **후보 보드** — 스캔 결과가 페이지를 나가면 사라지던 문제 해결. 발굴 결과에 ☆ 를 눌러 담아두면
  성장주 발굴 페이지 최상단에 **재무(성장 점수) × 타이밍(수급·추세) 2축 사분면**으로 표시된다:
  🎯 정밀 분석 대상 / 👀 관찰 — 타이밍 대기 / ⚠️ 수급만 좋음 — 테마성 의심 / 🗑 정리 후보.
  담을 때 성장 점수·배지·버핏 통과수·PEG·코멘트를 스냅샷으로 저장해 "언제 어떤 근거로 담았는지"가 남는다
  (`hooks/useCandidates.ts`, `components/CandidateBoard.tsx`)
  - **두 점수를 하나로 합치지 않는다** — "재무 91점 + 수급 이탈"을 평균 내면 거짓말이 되므로
    두 축을 그대로 두고 사분면으로 읽게 했다
  - 수급 판정은 기존 `/api/portfolio-verdicts` 재사용 — AI 호출도 인증 게이트도 없고 티커당 10분 캐시
  - 미국 후보는 국내 수급 데이터가 없어 **타이밍 축을 적용하지 않고** 따로 표시한다(없는 축을 흉내 내지 않음)
- 후보 저장은 `{v, data}` 형식으로 감쌌다 — 기존 훅들이 배열을 날것으로 저장해 스키마 변경 시
  조용히 깨지던 문제(완성도 점검 1차 A-6)를 이번엔 처음부터 방지. 파싱 실패 시 원본을
  `-corrupt-<ts>` 로 보존하고, 저장 실패(용량·시크릿 모드)를 화면에 알린다(A-5)
- 네이버 연간 재무 실패 시 **KIS 폴백** — 종목을 목록에서 통째로 떨어뜨리지 않는다.
  단 KIS 에는 컨센서스·다개년 시계열이 없어 점수가 낮게 나오므로 `limited` 경고를 함께 표시

### 🐛 수정
- **후보 보드 타이밍 축이 거의 발화하지 않던 문제** — 경량 판정이 정밀 분석과 같은 문턱(`score>=30`)을
  쓰면서도 재무등급·공시·정책·CIO·코스피 가점(최대 ~18점)을 입력에 넣지 않아, 같은 종목이라도
  점수가 체계적으로 낮게 나왔다. 실측 결과 시총 상위 30종목 중 `>=30`은 4종목(13%)뿐.
  기준을 `score>=10`(12종목·40%)으로 조정하고 4단계 라벨(매수우위/우호/중립/비중축소)로 세분화.
  조정 후 사분면 4/4가 모두 채워지는 것을 실측 확인

### 참고
- 배치 실측(국내 시총 상위 30종목)에서 축이 의도대로 작동: 셀트리온(성장 81·수급 +44)·
  한화오션(85·+13)이 🎯로, SK하이닉스(성장 91인데 수급 -40)가 👀로, 하나금융지주(성장 27·수급 +42)가
  ⚠️로 갈렸다. "재무는 좋은데 수급이 빠지는 종목"과 "수급만 좋은 종목"이 실제로 분리된다

## 2026-08-06 (4차)

### 🚀 배포
- 프로덕션 배포 (`9ef141e`) — 우주항공·방산 테마 보강

### ✨ 추가
- **우주항공·방산 테마 7 → 21종목** — 테마가 대형 방산 5개 중심이라 얇았다.
  - 방산 대형주: GD, LHX, TDG, HEI, LDOS, KRMN
  - 우주 발사·위성: **SPCX(SpaceX)**, ASTS, LUNR, RDW, PL, VOYG
  - 무인기·드론: KTOS, AVAV
  - 유니버스 127 → 141종목
- **SpaceX(SPCX)가 편입 가능해졌다** — 2026년 6월 나스닥 상장. 그 전까지 비상장이라
  담을 수 없던 종목이다. 프로덕션 API로 실제 시세·재무 조회를 먼저 확인한 뒤 반영
  (시총 $1.43T, 영업이익률 -41.6%, 포워드 PER 119.9 → 점수 15.6, "이익 적자" 경고)

### 참고
- 점수가 실적 유무를 정확히 갈라놨다: HEI 82.9 · RTX 76.5 · AXON 75.7(실적 있는 방산)
  vs SPCX 15.6 · VOYG 13.6 · ASTS 17(적자 우주주). 시총 $1.43T인 SpaceX가 하위권인 것은
  "규모가 아니라 이익"으로 채점한다는 설계 의도대로다

## 2026-08-06 (3차)

### 🚀 배포
- 프로덕션 배포 (`a964825`, `2a3b0c9`) — 미국 카테고리·종목 검색

### ✨ 추가
- **미국 종목 카테고리 (섹터 × 테마)** — 미국 유니버스가 고정 목록이라 "범위가 너무 넓다"는 문제 해결.
  토스·키움 등 증권사 분류를 참고해 2축으로 구성: **GICS 11섹터**(기술·커뮤니케이션·경기소비재·필수소비재·
  헬스케어·금융·산업재·에너지·유틸리티·소재·부동산) × **테마 17종**(AI·반도체, 반도체장비, 빅테크,
  클라우드·SaaS, 사이버보안, 데이터·인프라, 핀테크·결제, 전기차·자율주행, 우주항공·방산, 원자력·전력,
  양자컴퓨터, 비만치료제, 바이오·제약, 소비브랜드, 플랫폼·미디어, 금융·보험, 에너지·원자재).
  한 종목이 여러 테마에 속할 수 있고, 칩에 종목 수가 표시된다. 이제 "원자력·전력 7종목만" 같은 좁은 스캔이 가능
- 유니버스 82 → **127종목** — 양자컴퓨터·SMR·비만치료제·데이터센터 리츠 등 테마 커버리지 보강
- **종목 직접 검색** — 큐레이션 목록에 없어도 이름·티커로 찾아 그 종목만 스캔해 결과 맨 위에 추가('검색' 배지).
  미국은 기존 Yahoo 검색, 한국은 기존 종목 검색 재사용. 행 확장의 `#테마` 칩을 누르면 그 테마로 바로 필터

### 🐛 수정
- **종목명 자리에 티커가 찍히던 문제** — Yahoo quoteSummary 모듈 목록에 `price`가 빠져 있어
  longName/shortName을 못 받았다. 검색 종목이 "RBLX RBLX"로 보이던 원인이고,
  **기존 버핏 스크리너도 같은 이유로 계속 이름 대신 티커를 표시하고 있었다**(이번에 함께 복구 —
  "Microsoft Corporation", "NVIDIA Corporation"으로 정상 표시). (`lib/yahooFinance.ts`)

### 참고
- 테스트에 "모든 섹터·테마에 종목 1개 이상" 규칙을 넣었더니 **빈 '부동산' 섹터 칩을 실제로 잡아내**
  데이터센터 리츠(EQIX·DLR·AMT) 추가로 이어졌다. 테스트 32 → 34개

## 2026-08-06 (2차)

### 🚀 배포
- 프로덕션 배포 (`5b248c0`) — 성장주 발굴 2차: 시장환경·버핏체크·추천코멘트·미국주식

### ✨ 추가
- **시장 환경 패널** — 성장주 투자 "날씨" 5지표: VIX(변동성)·미국채 10년(할인율)·WTI 유가(비용/인플레)·
  달러인덱스(자금 흐름)·한국 기준금리(유동성). 지표별 우호/중립/경계 + 왜 중요한가 코멘트 + 종합 신호등.
  전부 기존 FRED·ECOS 키 재사용, 6시간 캐시. 배포 시점 실측에서 WTI 1개월 +17.8%가 경계로 실제 발화
  (`lib/marketEnvironment.ts`)
- **버핏 체크 7항목** — 학습 문서("버핏식 성장주 재무제표 보는 법") 기준을 각 종목에 자동 적용:
  ROE 유지·영업이익률 15%↑·흑자 지속·부채비율<100%·매출 성장·배당·당좌비율(미국은 FCF 플러스).
  한국은 FCF·이자보상배율이 네이버 연간 API에 없어 당좌비율·유보율로 대체(한계 명시)
- **룰 기반 추천 코멘트** — 종목마다 "최대 강점 + 최대 주의점" 1~2문장 자동 생성, 상위 추천 5 카드로 요약
- **미국주식 탭** — 대형주 큐레이션 82종목(메가캡·반도체·소프트웨어·결제·헬스케어·소비재·에너지).
  한국과 동일한 100점 4부문 체계로 나란히 비교 가능. 포워드 EPS 성장 = 트레일링/포워드 PER 격차,
  PEG는 Yahoo 제공값 우선, 적자 기업 PEG 미계산 원칙은 한국과 동일 (`lib/usGrowth.ts`)
- Yahoo 조회 로직을 버핏 스크리너에서 추출해 공용화 (`lib/yahooFinance.ts` — 스크리너 동작 동일)

### 참고
- 로컬 Node 24에서 Yahoo 홈의 거대 응답 헤더가 undici 한도를 넘어 로컬 검증 불가(HeadersOverflowError) —
  프로덕션(Vercel)에서는 정상. 검증은 배포 후 프로덕션 실측으로 수행(미국 15종목 1.2초, 실패 0)

## 2026-08-06 (1차)

### 🚀 배포
- 프로덕션 배포 (`9114fa1`) — 성장주 발굴 기능

### ✨ 추가
- **성장주·기대주 발굴 페이지 `/growth`** — KRX 시총 상위(50/100/150종목, KOSPI/KOSDAQ/전체)를
  버튼 한 번으로 스캔해 100점 점수로 줄 세운다. 확정 성장 35 + 미래 기대(애널리스트 컨센서스) 30 +
  수익성 15 + 밸류에이션 20. 배지 4종(고성장·기대주·턴어라운드·저평가성장), 필터(컨센서스만/흑자만/PEG≤1.5),
  행 확장 상세, 각 행에서 국내주식 정밀 분석으로 바로 이동. (`app/growth/`, `lib/growthScreener.ts`, `app/api/growth-scan/`)
  - 데이터는 네이버 연간 재무 1콜/종목(12시간 캐시) — 확정 3개년 + 컨센서스 추정 1개년.
    컨센서스의 PER 열이 곧 포워드 PER라 별도 계산이 필요 없다 (2026-08-06 실측 확인)
  - **PEG는 기준 EPS가 흑자일 때만 계산** — 적자 -160원 → +60원을 "137% 성장"으로 읽으면
    턴어라운드 종목이 초저 PEG로 둔갑한다. 구현 중 회귀 테스트가 실제로 이 버그를 잡았다
  - 우선주(코드 끝자리≠0)·스팩·리츠는 유니버스에서 제외
- 국내주식 분석 페이지가 `?ticker=` 딥링크를 받는다 — 선택만 세팅하고 자동 실행은 하지 않음(AI 비용 원칙)

## 2026-07-27

### 🚀 배포
- 프로덕션 배포 (`24ee41c`, `19d3977`) — 전체 점검 1·2차 수정

### 🔒 보안·법률
- **KRX API 키가 public 저장소에 하드코딩돼 있던 문제** — `process.env ?? '실제키'` 폴백 패턴이
  커밋 `3876676`부터 노출. 코드에서 제거했으나 git 이력에 남아 있어 **키 재발급 필요**(미완, data.krx.co.kr).
  (`app/api/krx/stock-list/route.ts`)
- AI 실과금 라우트 2종(`/api/stock-analysis`, `/api/coin-analysis`)과 디버그 라우트가 무인증 공개였던
  문제 — 미들웨어 게이트 확대. 배포본에서 401 실측 확인. 분석 페이지는 최초 1회 토큰 잠금해제 필요
- KIS 액세스 토큰을 무인증 반환하던 죽은 라우트 `/api/kis/token` 삭제 (호출부 0건)

### 🐛 수정
- **분할매수 3차 지정가가 손절선 밖에 걸리던 문제** (라이브 4코인 중 3개 재현) —
  눌림 대기 존이 15m EMA20을 그대로 써서 손절을 넘어갔다. 스톱 발동 후에만 체결되는 지정가는
  의도치 않은 재진입이 된다. 존 클램프 + `tranches3` 자체 방어(이중) (`lib/coinAnalysis.ts`, `lib/positionSizing.ts`)
- **필요 증거금이 시드를 넘어도 아무 경고가 없던 문제** — 손절폭 0.12%대에서 보수 배율 증거금이
  시드의 200%를 넘는 실행 불가 수치를 담담히 표시했다. 붉은 경고 + 대안 안내 (`app/coin-analysis/page.tsx`)
- **피보나치 확장이 2차 목표를 1차 목표보다 나쁘게 덮어쓰던 문제** (롱·숏 양쪽, 최악 0.2R) —
  '더 먼 목표'일 때만 채택. 손익비도 상수 1.5 대신 실제 계산값 (`lib/coinAnalysis.ts`)
- **레버리지 "신호 강도"가 점수와 무관하던 문제** — 코인 점수 22와 55가 같은 배율(4/8배)을 냈다.
  원인은 등급이 점수가 아니라 진입게이트 통과 여부의 별칭이었던 것. 점수 함수로 분리,
  게이트 미충족은 별도 0.5 승수. 진입조건 충족 시 배율은 이전과 동일
- **주식 백테스트가 어떤 종목이든 신호 0건이던 문제** — 수급 결측 차단(정당한 수정)이
  과거 수급이 없는 백테스트까지 죽였다. 백테스트 전용 면제 플래그 추가, 실시간 차단은 유지 (`lib/stockBacktest.ts`)
- **주식 목표가가 "∞원"으로 표시될 수 있던 문제** — 현재가 위 저항이 없으면 `Math.min(빈배열)`이
  Infinity(truthy)라 폴백이 불발. 익절 자동판정도 영원히 미달이었다 (`lib/stockAnalysis.ts`)
- **미국 CPI가 실제로는 13개월 변화율이던 문제** — 미공표월('.') 필터로 인덱스가 한 칸 밀림.
  화면 3.73% → 올바른 값 3.46%. 날짜 매칭으로 수정 (`lib/macroIndicators.ts`)
- **가계부채가 5분기 전(2025 Q1) 값에 고정되고 QoQ가 항상 비어 있던 문제** — ECOS가 오래된
  분기부터 반환하는데 조회 건수 10건이 최신 분기를 잘라냈다. 2026 Q1·QoQ +0.71%로 복구
- **분석 실패가 화면에 전혀 안 보이던 문제** — 서버 타임아웃 시 직전 분석이 현재 판정처럼
  남았다. 실패 배너 + "이전 분석이니 주문 근거로 쓰지 말 것" 경고 + 재시도 버튼 (`lib/fetcher.ts`, 분석 2페이지)

### ✨ 추가
- 백업이 매매일지·실보유 포트폴리오를 빼놓던 문제 — `kospi-lab-` 접두사 전체를 담는 v2 포맷으로
  (구버전 백업 파일도 복원 가능). 링크가 없어 못 찾던 백업 화면(`/virtual`)을 내비 "내 자산"에 노출
- 매매일지 개별·전체 삭제에 확인 대화상자 (되돌리기가 없으므로)
- 머니매스 회귀 테스트 15 → 27개 — 실사용 점수 구간(20~60)을 겨냥한 케이스 보강

## 2026-07-16

### 🚀 배포
- 프로덕션 배포 (`7379325`) — 완성도 7축 개선

### ✨ 추가
- 머니매스 회귀 테스트 15케이스 (`tests/engine.test.ts`, `lib/positionSizing.ts` 분리) — 커밋 전 `npm test` 필수화
- 크래시 복구 화면(라우트 404·전역 오류), robots/sitemap, 페이지별 브라우저 탭 제목
- 전역 푸터에 투자 면책 + 선물 원금 초과 손실 고지
- 매매일지에 실현손익(USDT) 기록 — 엔진 판정 vs 실제 성적 비교 가능

## 2026-07-15

### 🔄 변경
- **코인 방향 문턱 ±30 → ±20** (`adcc509`) — "계속 관망만 나온다"는 실투자 피드백 반영.
  약한 우위도 방향 표시, 관망 시에도 기울기 바 표시. 진입 초록불(45)은 유지해 남발 방지

## 2026-07-14 ~ 07-10

### ✨ 추가 (코인선물 실전 도구화)
- 진입 플랜 요약(얼마에/어디서 손절) · 신호 안정성 지표 · 분할 매수 3분할 (`bde0a2c`, `02bc49a`)
- 상위 타임프레임(4H·1D) 레짐 필터 · 진입자리 품질 게이트 · 오더북 유동성 패널 (`075b425`)
- 실시간 시세 분리 폴링 + 레버리지 3요소 동적 계산 (`6e34ace`)
- AI 브리핑 모델 선택(Haiku/Sonnet/Opus) · 홈 분석 카드 · USDT/KRW 김프 환율 pill
- 잠금 해제 화면 폼 (`4ffe589`) — 토큰 URL 수동 입력 불편 해소

### 🔄 변경
- **분석은 페이지 진입이 아니라 버튼을 눌러야 실행** (`4455bb9`) — AI 호출 비용 절약 원칙 확립

### 🐛 수정
- AI 브리핑이 항상 비어 있던 문제 — 원인은 Anthropic 크레딧 부족(400). 키·모델 문제 아님.
  에러 본문 로깅 추가, 크레딧 충전 시 자동 복구 (`7638ce2`)
- 수급 데이터 없는 종목이 매수판정을 통과하던 문제 — 결측을 중립 취급하던 조건 수정 (`7ba5064`)
